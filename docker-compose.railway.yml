services:
  postiz:
    image: ghcr.io/gitroomhq/postiz-app:latest
    restart: always
    environment:
      # URLs - Railway gera automaticamente
      MAIN_URL: ${RAILWAY_STATIC_URL}
      FRONTEND_URL: ${RAILWAY_STATIC_URL}
      NEXT_PUBLIC_BACKEND_URL: ${RAILWAY_STATIC_URL}/api
      JWT_SECRET: "znuRXfckJe/T7OCVEGQgoB2xo+iGRzYAtc4D7u7JN07jBccQRNs3M8ADoUehangouXJqv/famV5R2sGlIUP0Lw=="
      
      # Railway fornece estas automaticamente
      DATABASE_URL: ${DATABASE_URL}
      REDIS_URL: ${REDIS_URL}
      
      BACKEND_INTERNAL_URL: "http://localhost:3000"
      IS_GENERAL: "true"
      DISABLE_REGISTRATION: "true"
      
      # Storage
      STORAGE_PROVIDER: "local"
      UPLOAD_DIRECTORY: "/uploads"
      NEXT_PUBLIC_UPLOAD_DIRECTORY: "/uploads"
      
      # Segurança habilitada para produção
      
      # TikTok API
      TIKTOK_CLIENT_ID: "Organization ID"
      TIKTOK_CLIENT_SECRET: "7542693647393981445"
    volumes:
      - uploads:/uploads
    ports:
      - 5000:5000

volumes:
  uploads:
